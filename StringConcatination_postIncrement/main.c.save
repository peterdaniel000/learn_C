//String concatenation using postIncrement operator
//Date: 23-12-2024
//Author: Peter Daniel
#include <stdio.h>
#include <stdlib.h>
#define MAX_SIZE 100

void stringconcatenation(char s[], char t[]);
int main()
{
    char str1[MAX_SIZE], str2[MAX_SIZE];
    printf("Write the first string \n");
    scanf("%s",str1);
    printf("Write the second string to be appended \n");
    scanf("%s",str2);
    stringconcatenation(str1, str2);
    return 0;
}


void stringconcatenation(char s[], char t[])
{
    int i=0, j=0;
    while(s[i]!='\0')
        i++;

    while((s[i++] = t[j++])!='\0')
    printf("Appended string is %s \n", s);

}
